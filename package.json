{
  "name": "learning-copilot",
  "version": "1.0.0",
  "description": "Personalized Learning Copilot - AI-powered learning assistant",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && python app.py\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && python app.py",
    "build": "cd frontend && npm run build",
    "build:backend": "echo 'Backend ready for deployment'",
    "start": "cd backend && python app.py",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && pip install -r requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": "24.x",
    "python": "3.9"
  }
}
